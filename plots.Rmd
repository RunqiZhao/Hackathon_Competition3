---
title: "Untitled"
author: "Zhe"
date: "2021/10/22"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
setwd('C:\\Users\\13127\\Desktop\\datasets\\Hackathon_Competition3')
load('combined_data.rData')
```

## R Markdown

```{r cars}
job_skill <- job %>% group_by(State,Skill) %>% summarise(n=n()) %>% arrange(State,-n)
job_certi <- job %>% group_by(State,Certification) %>% summarise(n=n()) %>% arrange(State,-n)
```
```{r}
ggplot(data = subset(job_skill[1:round(length(job_skill$State)*0.002),],State == 'Alabama',), mapping=aes(n,reorder(Skill,n), fill=-n)) +
  geom_col()+
  xlab("count")+
  ylab("skill")+
  theme(legend.position = 'none')
  
```

```{r}
ggplot(data = subset(job_certi[1:round(length(job_certi$State)*1),],State == 'Alabama',), mapping=aes(n,reorder(Certification,n), fill=-n)) +
  geom_col()+
  xlab("count")+
  ylab("skill")+
  theme(legend.position = 'none')
```



## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
